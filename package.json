{
  "name": "tapterm",
  "version": "0.0.2",
  "private": true,
  "description": "Web terminal + streamed host browser panes (Playwright)",
  "main": "server.js",
  "scripts": {
    "dev": "PORT=8049 node --watch server.js",
    "start": "node server.js"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "playwright": "^1.58.2",
    "ws": "^8.19.0"
  }
}
